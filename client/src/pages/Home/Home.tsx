import Posts from "../../components/Posts/Posts";

import "./home.scss";

const Home = (): JSX.Element => {
  return (
    <div className="home">
      <Posts />
    </div>
  );
};

export default Home;
